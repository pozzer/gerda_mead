.box.box-primary
  .box-header.with-border
    %h3.box-title Novo
  / /.box-header
  / form start
  = simple_form_for [current_account, @resource_type] do |f|
    .box-body
      = f.association :resource_class

      .form-group
        = f.label :url
        .input-group
          %span.input-group-addon https://example.com
          = f.input_field :url, class: "form-control"

    / /.box-body
    .box-footer
      = f.button :button do
        %i.fa.fa-plus
        Salvar
      %a{href: account_resource_types_path(current_account)} Cancelar
